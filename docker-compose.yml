services:
  system-agent-rag:
    image: system-agent-rag:latest
    build: .
    container_name: system-agent-rag
    restart: unless-stopped
    volumes:
      # Mount the directories you want to watch (read-only)
      - /Users/cengsin/GoWork2026:/GoWork2026:ro
      - /Users/cengsin/GolandWorkSapce2025:/GolandWorkSapce2025:ro
      - /Users/cengsin/GolandWS:/GolandWS:ro
      # Mount the output directory (read-write)
      - /Users/cengsin/work_description:/output
      # Mount config file
      - ./config-docker.yaml:/etc/system-agent-rag/config.yaml:ro
